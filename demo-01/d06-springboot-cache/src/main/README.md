



```txt
这里重点要注意的是：
JobManager 与TaskManager 是不同的进程
但是我们的代码是写到一起的，它是被flink 处理并分发到不同的机器实例上面，最后在对应的机器实例上面启动了进程。
所以要注意spring boot 容器的创建与对应的生命周期，与使用范围。
特别是本地启动的问题，本地因为是默认多并行度，而spring boot web 配置的是同一个端口，所以在本地运行时要注意将并行度设置为1 ，或者单独处理WEB 的监听端口。
```

